name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        run: |
          curl -sL https://services.gradle.org/distributions/gradle-8.5-bin.zip -o /tmp/gradle.zip
          unzip -q /tmp/gradle.zip -d /opt
          echo "/opt/gradle-8.5/bin" >> $GITHUB_PATH

      - name: Install Wine for jpackage
        run: sudo apt-get update && sudo apt-get install -y wine64

      - name: Build HMCL JAR
        run: gradle :HMCL:build -x test --no-daemon

      - name: Build EXE with jpackage
        run: |
          JAR=$(ls HMCL/HMCL/build/libs/*.jar | head -1)
          jpackage \
            --input "$(dirname $JAR)" \
            --main-jar "$(basename $JAR)" \
            --name Overworld-Launcher \
            --dest Overworld-Launcher/build/libs \
            --java-options "-Xms256m" \
            --java-options "-Xmx1024m" \
            --app-version "${{ github.run_number }}" \
            --vendor "Overworld Launcher" \
            --description "Overworld Launcher"

      - name: Upload HMCL jar
        if: success()
        uses: actions/upload-artifact@v6
        with:
          name: Overworld-Launcher-fat-jar
          path: HMCL/HMCL/build/libs/*.jar
          retention-days: 7

      - name: Upload HMCL EXE
        if: success()
        uses: actions/upload-artifact@v6
        with:
          name: Overworld-Launcher-windows-exe
          path: Overworld-Launcher/build/libs/*.exe
          retention-days: 7
