/*
 * Hello Minecraft! Launcher - HMCLaF module
 */
if (System.getenv("BUILD_NUMBER") != null)
    version = System.getenv("BUILD_NUMBER")

dependencies {
    implementation project(':HMCLAPI')
    implementation project(':HMCLCore')
}
